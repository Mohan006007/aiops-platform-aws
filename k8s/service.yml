apiVersion: v1
kind: Service
metadata:
  name: aiops-service
  labels:
    app: aiops
spec:
  type: LoadBalancer
  selector:
    app: aiops
  ports:
    - name: http
      port: 80
      targetPort: 5000
    - name: metrics
      port: 8000
      targetPort: 8000