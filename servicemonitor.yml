apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: aiops-monitor
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: aiops
  endpoints:
  - port: metrics
    path: /metrics
    interval: 15s